DJANGO_SECRET_KEY=change-me-in-prod
DJANGO_DEBUG=false
DJANGO_ALLOWED_HOSTS=*

# Provider this container serves; routes mount under /adapters/<provider>/*.
# Deploy one container per provider (aws, gcp, azure, ...).
ADAPTER_PROVIDER=aws

ADAPTER_DB_NAME=adapter_db
ADAPTER_DB_USER=adapter_user
ADAPTER_DB_PASSWORD=isis2503
ADAPTER_DB_HOST=localhost
ADAPTER_DB_PORT=5432

NORMALIZATION_SERVICE_URL=http://localhost:8081/normalize
REQUEST_TIMEOUT_SECONDS=10
